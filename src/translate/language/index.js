import { messages as portugueseMessages } from './pt';
import { messages as englishMessages } from './us';
import { messages as spanishMessages } from './es';

const messages = {
  ...portugueseMessages,
  ...englishMessages,
  ...spanishMessages,
};

export { messages };
